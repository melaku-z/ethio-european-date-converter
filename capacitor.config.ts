import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'app.calendar.ethiopian.converter',
  appName: 'Ethiopian Calendar Converter',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
}

export default config
